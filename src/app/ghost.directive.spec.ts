import { GhostDirective } from './ghost.directive';

describe('GhostDirective', () => {
  it('should create an instance', () => {
    const directive = new GhostDirective();
    expect(directive).toBeTruthy();
  });
});
